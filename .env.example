DATABASE_URL=postgresql+psycopg://devops:devops123@127.0.0.1:5433/log_analyzer

# Optional real AI mode. Leave empty to use local ML fallback only.
# When set, the backend runs local analysis first, then asks OpenAI to refine
# root cause, SLO impact, service impact, and runbook guidance.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
